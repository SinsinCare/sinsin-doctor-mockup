# 의사 대시보드 React 재구현 견적 및 API 계약 검토

작성일: 2026-07-30

기준: 배포 데모, `HANDOFF.md` M1~M5 및 인수 기준, `API.md` v0.1

## 1. 산정 전제

- 프론트엔드 1명이 React 18 + TypeScript + Vite로 참조 구현을 재구현한다.
- 검수 기준은 픽셀 일치가 아니라 `HANDOFF.md` 6장의 동작 동치다.
- 기존 목업의 화면 구조, SVG 좌표계, 계산 규칙과 디자인 토큰을 재사용한다.
- 백엔드 개발, 인프라 운영, 데이터 마이그레이션은 제외한다. 실제 API 연동과
  계약 조율은 M5에 포함한다.
- 단가는 1 MD(man-day)당 80만 원, 부가세 별도 기준이다.

## 2. M1~M5 견적

| 마일스톤 | 범위 | 기간 | 비용 |
|---|---|---:|---:|
| M1 — 셸·읽기 전용 | 로그인·OTP, 앱셸, 환자 목록/전환, 공통 헤더, 총평·TOP3, mock API | 2.5~3 MD | 200~240만 원 |
| M2 — 차트 | 프리셋·실측/%·기간 전환, 주 평균, 문제 구간, 거터 라벨, 해석 블록 | 3.5~4.5 MD | 280~360만 원 |
| M3 — 쓰기 | 제한값 즉시 재계산/저장, 수치 수정, SOAP·서명, 실천 과제, 리포트 구성·전송 | 3~4 MD | 240~320만 원 |
| M4 — 식단·연결·설정·PDF | 식단 마스터-디테일, TOP5, 연결 요청, 설정 모달, A4 1페이지 PDF | 3.5~4.5 MD | 280~360만 원 |
| M5 — 실제 API 전환 | mock 제거, 실제 API/인증/오류 상태 연동, 캐시 키 정리, CGM 예약 계약 확인 | 1.5~2 MD | 120~160만 원 |
| **합계** |  | **14~18 MD** | **1,120~1,440만 원** |

캘린더 기간은 피드백과 API 준비 시간을 포함해 약 3~4주다. 로그인 자체는
0.5 MD 안팎이며, M1의 나머지는 환자 전환 캐시·공통 헤더·읽기 전용 대시보드
구성에 대한 산정이다.

## 3. 마일스톤별 완료 조건

- M1: 환자 전환 시 해당 환자 데이터만 보이고 공통 헤더와 총평이 함께 바뀐다.
- M2: 프리셋·모드·기간 전환과 35일 초과 주 평균 규칙이 데모와 동치다.
- M3: 제한 저장 직후 차트, 초과일, 요약과 총평이 같은 화면에서 재계산된다.
  서명된 SOAP 메모는 수정되지 않는다.
- M4: 기록일을 선택하면 우측 상세가 바뀌고 미기록일은 선택되지 않는다.
  선택한 실천 과제만 A4 1페이지 환자용 PDF에 출력된다.
- M5: mock과 실제 API의 응답 차이가 없고, 로딩·빈 상태·오류·재시도가
  환자 단위 캐시를 오염시키지 않는다.

공통 완료 조건은 820~1440px에서 가로 스크롤이 없고, 당뇨 여부에 따른 혈당
모듈 노출과 폴백을 포함한 `HANDOFF.md` 6장의 인수 기준을 모두 통과하는 것이다.

## 4. API 계약 수정 제안

### 확정 전에 반영할 항목

1. **코드형 enum으로 통일**
   - `CkdStage`는 `G2 | G3A | G3B | G4 | G5`로 맞춘다.
   - `cause`, 메모 유형, 식사 슬롯, 과제 출처는 한글 표시 문자열 대신
     영문 코드와 별도 label을 사용하고 `OTHER`/`UNKNOWN`을 예약한다.

2. **파생 필드의 기준값 명시**
   - `age` 대신 `birthDate`, `bmi` 대신 서버 계산값과 계산 시점을 제공한다.
   - `isDiabetic`와 `cause`가 충돌할 때 어떤 필드가 UI 노출의 기준인지
     명시한다. 현재 UI 스위치는 `isDiabetic`을 기준으로 하는 편이 안전하다.

3. **날짜·시간·조회 구간 규칙 고정**
   - 시각은 UTC ISO 8601, 환자 일자는 병원 기준 타임존의 `YYYY-MM-DD`로 한다.
   - `from`, `to`의 포함 여부와 기본 정렬을 명시한다.
   - `recorded=false`인 날의 영양값은 0이 아니라 `null` 또는 생략으로 표현해
     실제 0 섭취와 미기록을 구분한다.

4. **페이지네이션과 응답 envelope 추가**
   - 환자, 메모, 연결 요청, 식단 기간 조회에 `items`, `nextCursor`,
     `hasNext`를 공통 적용한다.
   - 모든 응답에 `requestId`를 포함해 운영 로그와 화면 오류를 연결한다.

5. **동시 수정과 중복 요청 보호**
   - 제한값, 과제, 프로필에는 `version` 또는 ETag와 `If-Match`를 사용한다.
   - 메모 서명, 연결 수락/거절, 리포트 전송은 `Idempotency-Key`를 받는다.
   - 이미 처리됐거나 서명된 리소스는 `409 CONFLICT`로 일관되게 응답한다.

6. **서버가 작성자를 확정**
   - `updatedBy`, `signedBy`, 연결 처리자는 요청 body에서 받지 않고 JWT의
     사용자로 서버가 기록한다.
   - 제한 변경, 수치 수정, 서명, 연결 처리와 리포트 전송은 감사 로그 대상이다.

7. **인증 계약 보완**
   - `/auth/refresh`, access/refresh token 만료, refresh 회전·폐기, OTP 재전송과
     시도 제한, 비밀번호 변경 후 세션 정책을 추가한다.
   - 인증 오류는 `401`, 권한 부족은 `403`으로 분리한다.

8. **오류 형식 표준화**
   - `{ code, message, fieldErrors?, requestId }` 형태를 공통으로 사용한다.
   - 입력 오류 `400/422`, 동시 수정 `409`, 잠긴 메모 `409`, 요청 제한 `429`의
     처리 기준을 문서화한다.

9. **사진과 민감정보 전달 방식**
   - `photoUrl`은 만료되는 서명 URL과 `expiresAt`을 반환한다.
   - 환자명·의료정보·토큰이 URL, Cloud 로그, 분석 이벤트에 남지 않도록
     로깅/마스킹 규칙을 계약에 포함한다.

10. **리포트·insights의 재현성**
    - `/insights`는 계산에 사용한 `from`, `to`, `recordedDays`,
      `limitsVersion`을 함께 반환한다.
    - 리포트 전송 응답은 `reportId`, `status`, `createdAt`을 반환하고
      PDF 출력과 환자 앱 전송이 같은 snapshot을 사용하게 한다.

### 권장 응답 예시

```json
{
  "data": {
    "items": [],
    "nextCursor": null,
    "hasNext": false
  },
  "meta": {
    "requestId": "req_01J...",
    "generatedAt": "2026-07-30T08:00:00Z"
  }
}
```

## 5. 계약 확정 순서

1. enum, 날짜·타임존, nullability, 오류 envelope를 먼저 확정한다.
2. 목록형 API의 cursor 페이지네이션과 정렬 기준을 확정한다.
3. 제한 저장·메모 서명·연결 처리·리포트 전송의 동시성/멱등성을 확정한다.
4. M1~M4는 mock adapter로 개발하고, M5에서 adapter만 실제 API로 교체한다.
5. CGM은 현재 예약 계약을 유지하되 도입 시 별도 버전으로 확장한다.
