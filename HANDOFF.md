# 신신당부 의사 대시보드 — 프론트엔드 개발 핸드오프

> **이 저장소의 `index.html`은 참조 구현(reference implementation)입니다. 이어서 개발하지 말고, 아래 스택으로 재구현하세요.**
> 화면·인터랙션·데이터 규칙의 최종 기준은 배포된 데모입니다. 문서와 데모가 다르면 데모가 맞습니다.

- 실사용 레이아웃(풀폭): https://sinsincare.github.io/sinsin-doctor-dashboard/
- 시연 레이아웃(센터드 프레임): https://sinsincare.github.io/sinsin-doctor-dashboard/demo.html
- 기능 명세(결정 이력·임상 근거 포함): Notion "기능명세서 v2" — 접근 권한은 발주자에게 요청
- 디자인 파일: Figma `1mW13xV8NormDrZQIjtbw1` (메디올로지 팀)
- API 계약 초안: [`API.md`](API.md)

데모 계정: `jhlee@kidneasy.com` / `kiney1` (입력값 무관 통과, 2단계 인증 OTP 프리필)

---

## 1. 권장 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | **React 18 + TypeScript + Vite** | |
| 서버 상태 | **TanStack Query(React Query)** | 환자 전환 시 캐시 단위 = patientId |
| 클라이언트 상태 | Zustand 또는 Context (얕음: 선택 환자·프리셋·기간·모드 정도) | |
| 차트 | **SVG 직접 렌더** (viewBox 720×380 좌표계 그대로 이식) | 참조 구현의 `draw()`/`yScale()`/`intakeItems()` 로직이 곧 스펙. D3 스케일만 빌려도 됨. 무거운 차트 라이브러리 금지(듀얼 트랙·거터 라벨·문제 구간 밴드 커스텀이 핵심) |
| 스타일 | CSS 변수 기반 토큰 + CSS Modules(또는 vanilla-extract) | 토큰은 §4 표가 전부. Tailwind 쓰려면 토큰을 theme로 이식 |
| 폰트 | Pretendard Variable | |
| 인쇄 | PDF 리포트 = print CSS + `window.print()` (참조 구현 방식 유지) | 서버 PDF 생성 불필요 |

## 2. 화면 구조 (4뷰 + 로그인, 배타적 전환)

```
로그인 (이메일/비밀번호 → OTP 2단계) ── 딥링크(?view=)는 인증 후 진입 가정
└─ 앱셸: 사이드바(216px: 네비 + 환자 목록) + 메인(내부 스크롤)
   ├─ 홈 ─ 환자 헤더(배지·검사 타일·제한 패널) 는 4뷰 공통 고정
   │      ├ 검사×섭취 통합 차트 (프리셋 K/P/단백질/Na + 당뇨만 혈당, 실측/% 모드, 기간 1주~6개월+커스텀)
   │      ├ 해석 블록(상태/식이 연관성/관리) · 문제 식사 카드
   │      ├ 우측: 식이 분석 총평(6~7행) · 고위험 식사 TOP3 · 피해야 할 음식
   │      └ 하단: 환자 실천 과제 · 검사 일정(D-day) · 복용약(순응도)
   ├─ 식단 상세 ─ 요약 스탯 4타일 → 기준 초과 TOP5(가로 스트립, 0~5개 적응형)
   │      → 일별 기록 표(기간 컨트롤·행 클릭) ↔ 우측 그날 상세 패널(마스터-디테일, sticky)
   ├─ 진료 메모 ─ 메모 이력(검색·유형) + SOAP 에디터(O 자동삽입, P→실천과제/환자코멘트, 전자서명 잠금)
   └─ 설정 ─ 프로필(수정 모달) · 환자 연결(검색 노출·요청 이력) · 알림 임계값 · 보안(비밀번호·로그아웃)
   + 알림 패널(연결 요청 수락/거절) · 모달: 수치 수정 / 식사 상세 / 프로필 / 이력 / 비밀번호
```

## 3. 핵심 데이터 규칙 (서비스 로직 — 참조 구현 `build()`에서 검증 가능)

- **단백질 기준**: 단백뇨 A3 → 0.6 g/kg, 그 외 0.8 g/kg. 화면에는 항상 `g/kg = g/일(체중 환산)` 병기
- **수분**: 기본 "기록만". 의사가 제한(mL) 설정 가능. 임상 기본값: G5·부종(체중 급증) 환자는 제한
- **의사 설정 제한 저장** → 차트 제한선·초과 막대·주간 요약·총평 즉시 재계산 (클라이언트 재계산 + 서버 저장)
- **혈당 모듈은 원인질환이 당뇨인 환자에게만** 노출(차트 프리셋·헤더 HbA1c 타일·총평 행). 비당뇨는 이벤트성 선별 알림만(API.md 참조). G4~5 당뇨는 "A1c 과소평가 가능" 캡션 필수
- **초과일 판정**: 해당 영양소 일 섭취 > 의사 설정 제한. 주간 집계는 조회 창 35일 초과 시 주 평균
- **배지 시맨틱**: 그레이=정보 · 옅은 앰버 틴트=주의 · 진한 틴트=위험. 배지에 영양소 식별색 사용 금지
- **미기록일**: 차트 그레이 스텁, 표 "기록 없음", 일별 상세 클릭 불가. 분석 문구는 기록일만으로 계산
- **PDF 리포트 = 환자용**: 그래프 없음, 쉬운 말 풀이, 체크된 실천 과제만, 다음 진료 D-day, A4 1페이지 고정

## 4. 디자인 토큰 (v3 — One Primary + Grayscale · Borderless · Shadowless)

```css
/* 프라이머리 */
--blue:#3563E9; --blue-deep:#2A4FC4; --blue-tint:#EDF2FF; --blue-tint2:#E2EAFE;
/* 그레이 스케일 (라이트) */
--ground:#EDEFF4; --surface:#FFFFFF; --g50:#FAFBFC; --g75:#F4F5F8; --g100:#EDEFF4; --g200:#E1E4EB; --g300:#C8CDD9;
--ink:#1A2032; --ink2:#666E82; --ink3:#9CA3B5;
/* 임상 경고 전용 (장식 금지) */
--accent:#EE8A2F; --accent-deep:#AC5701; --accent-bg:#FCF1DF; /* 위험 배지 틴트: #F7DFC0 / #8A4504 */
/* 데이터 식별 6색 — 잉크 OKLCH L=0.550 정렬, 배경 L≈0.96 (배지·위계 용도 금지) */
단백질  #7052E0 / #F4EFFF     Na  #8D6C06 / #FDF5AF     K   #19864F / #E7F7EE
P      #BC3A64 / #FFEDF4     수분 #1D66B8 / #E3EEFB     혈당 #BC461E / #FEECE5
/* 차트 시리즈 (CVD 검증 완료 — 변경 시 재검증 필요) */
--lab:#3563E9(라이트)/#7D8DDE(다크); --intake:#8FA9F2(라이트)/#4656C2(다크);
```

- **보더·섀도 전역 금지.** 구분은 면 톤 4단계: 캔버스 g100 > 카드 흰색(패딩 20px 통일) > 블록 g75 > 인풋·칩 g100
- 입력: g100 필드 필 + 포커스 시 `outline:2px --blue`. 표: 행 보더 없이 지브라(g50) + 헤더 하단 1px(g200)
- 칩 높이 26px 통일. 라운딩: 카드 16 / 블록 12 / 칩 999. 간격 4pt 그리드. 이모지 금지(16px 스트로크 SVG)
- 다크 모드: 토큰 재정의로 대응(참조 구현의 `[data-theme]`·`prefers-color-scheme` 블록 참조)
- 브레이크포인트: **1280**(3열→2열) → **1080**(주요 그리드 1열) → **960**(2열→1열) → **900**(사이드바 숨김·세로 스크롤)

## 5. 마일스톤 제안

1. **M1 — 셸·읽기 전용**: 로그인, 앱셸, 환자 목록/전환, 헤더, 총평·TOP3 (mock API로)
2. **M2 — 차트**: 통합 차트(프리셋·모드·기간·주 집계·문제 구간 밴드·거터 라벨), 해석 블록
3. **M3 — 쓰기**: 제한 편집 저장, 수치 수정, SOAP 메모·서명, 실천 과제, 리포트 구성·전송
4. **M4 — 식단 상세·연결·설정·PDF**: 마스터-디테일, TOP5, 연결 요청 흐름, 설정 3모달, 환자용 PDF
5. **M5 — 서버 연동 전환**: mock → 실제 API (API.md 계약 기준), CGM 인터페이스 예약 확인

## 6. 인수 기준(Acceptance)

각 기능은 **데모와 픽셀이 아니라 동작으로 동치**면 통과입니다. 예:
- K 제한을 2,000→1,500으로 저장하면 초과 막대 수가 즉시 늘어난다
- 비당뇨 환자로 전환하면 혈당 프리셋 버튼이 사라지고 보던 혈당 차트는 칼륨으로 폴백한다
- 실천 과제 1개만 체크하고 PDF를 뽑으면 리포트에 그 1개만 나온다
- 820~1440px 어느 폭에서도 가로 스크롤이 생기지 않는다
